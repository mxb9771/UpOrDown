class Stock:

    # we want to format for
    def __init__(self, name, opens, highs, lows, closes, adjCloses,  volumes):
        self.name = name
        self.opens = opens
        self.highs = highs
        self.lows = lows
        self.closes = closes
        self.adjCloses = adjCloses
        self.volumes = volumes

